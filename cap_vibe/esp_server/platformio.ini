[env:esp32c3]
platform = espressif32
board = dfrobot_beetle_esp32c3
framework = arduino

; Upload settings
upload_speed = 1500000
monitor_speed = 115200

; No GPS, no Async WebServer. NimBLE for BLE server (phone) + BLE client (OBD2).
lib_deps =
  ArduinoJson
  h2zero/NimBLE-Arduino@^2.1.0
